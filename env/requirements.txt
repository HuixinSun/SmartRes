# Exact versions this release was evaluated with.
transformers==4.52.4
tokenizers==0.21.1
peft==0.15.2
accelerate==1.7.0
datasets==3.6.0
pydantic==2.10.6
sentencepiece==0.2.1
tiktoken==0.12.0
einops==0.8.1
safetensors==0.7.0
numpy==2.2.6
pillow==11.3.0
omegaconf
pyyaml
tqdm

# LLaMA-Factory requires these whenever predict_with_generate is on, which the
# evaluation configs set. Same set as its own "metrics" extra.
jieba==0.42.1
nltk==3.9.1
rouge-chinese==1.0.3
